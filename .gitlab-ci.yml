stages:
  - build
  - test
  - deploy

variables:
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: "/certs"

# Используем Docker-in-Docker (dind) для работы с Docker Compose
services:
  - docker:19.03.12-dind

# Docker image, используемый для выполнения команд
image: docker:latest

# Сборка контейнеров с помощью docker-compose
build-job:
  stage: build
  script:
    - docker-compose -f docker-compose.yml build

# Тестирование FastAPI
test-job:
  stage: test
  script:
    - docker-compose -f docker-compose.yml run --rm fastapi pytest

# Деплой
deploy-job:
  stage: deploy
  script:
    - docker-compose -f docker-compose.yml up -d
